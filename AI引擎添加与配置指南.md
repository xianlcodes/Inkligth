# AI 引擎添加与配置指南

> **适用读者：** 所有用户，无需编程背景
>
> 本指南将一步步教你如何在平台中添加和配置AI引擎，让翻译、文献摘要、智能问答等功能正常运行。

---

## 什么是AI引擎？

简单来说，AI引擎就是平台与各种大语言模型之间的"桥梁"。配置好AI引擎后，平台才能调用这些模型为你提供以下服务：

| 功能 | 需要AI引擎吗？ |
|------|:---:|
| 文献翻译 | ✅ 必须 |
| 生成摘要 | ✅ 必须 |
| 创新点提取 | ✅ 必须 |
| 智能问答 | ✅ 必须 |
| 关键词提取 | ✅ 必须 |

平台支持接入多种AI服务商（如DeepSeek、OpenAI、通义千问、智谱GLM等），你可以自由选择使用哪家的模型。甚至可以添加多个引擎，在翻译时根据需要切换。

---

## 在哪里配置？

1. 登录平台后，点击左侧菜单栏中的「**系统设置**」
2. 在设置页面中找到「**AI引擎配置**」标签页（通常排在靠前的位置）
3. 进入后你会看到已配置的引擎列表（首次使用时列表为空）
4. 点击右上角蓝色的「**添加引擎**」按钮，即可开始配置

> **截图提示：** 截取AI引擎配置页面的整体界面，标注"添加引擎"按钮的位置。

---

## 配置表单逐项说明

点击「添加引擎」后，会弹出一个对话框，里面有6个需要填写的项目。下面逐一说明。

### 1. 供应商

**这是什么？**
就是你选择使用哪家公司的AI服务。

**如何填写？**

点击下拉框，可以看到四个预设选项。你也可以直接在框里输入其他供应商的名称。

| 预设选项 | 对应公司 | 推荐理由 |
|----------|----------|----------|
| DeepSeek | 深度求索 | 性价比最高，中文能力强，新用户有免费额度 |
| OpenAI | OpenAI 公司 | 全球最知名的AI服务商，模型能力强 |
| Qwen (通义千问) | 阿里云 | 中文能力强，国内访问快 |
| GLM (智谱) | 智谱AI | 中文能力强，有完全免费的模型可用 |

**如果你要添加其他供应商（如KIMI、百度文心一言等），直接在框内输入名称即可。**

**选择建议：**
- 如果你是第一次使用，推荐选择 **DeepSeek**——它注册简单、有免费额度、中文效果好
- 如果你已有OpenAI账号和API Key，直接选择 **OpenAI**

### 2. API 地址

**这是什么？**
这是平台向AI服务商发送请求的网络地址。你可以把它理解为"给AI发消息需要寄到哪个地址"。

**如何填写？**

当你选择了供应商后，这个地址**会自动填写**：

| 供应商 | 自动填入的地址 |
|--------|---------------|
| DeepSeek | `https://api.deepseek.com` |
| OpenAI | `https://api.openai.com/v1` |
| Qwen (通义千问) | `https://dashscope.aliyuncs.com/compatible-mode/v1` |
| GLM (智谱) | `https://open.bigmodel.cn/api/paas/v4` |

**大多数情况下，你不需要修改这个地址。** 只有在以下情况才需要手动调整：
- 你使用的是通过第三方代理中转的API服务（代理商会提供给你一个新的地址）
- 你使用的是阿里云百炼平台的国内专属地址

**注意事项：**
- 地址必须以 `https://` 开头
- 从服务商官网获取的地址直接填入即可，不要随意增删字符
- 如果不小心改错了，重新选择一下供应商即可恢复默认地址

### 3. API Key

**这是什么？**
API Key 是你的"身份凭证"，就像一把钥匙。有了它，AI服务商才知道是谁在调用服务、该向谁收费。**必须妥善保管，不要泄露给他人。**

**如何获取？**

不同供应商的获取方式略有不同，以下是四个预设供应商的获取步骤：

#### 获取 DeepSeek API Key

1. 访问 [DeepSeek 开放平台](https://platform.deepseek.com/)
2. 点击右上角「注册/登录」，用手机号或微信注册账号
3. 登录后，点击左侧菜单的「**API Keys**」
4. 点击「**创建API Key**」按钮，输入一个名称（如"我的文献平台"）
5. 点击确认后，**立即复制显示的密钥并保存**——这个密钥只显示一次，关闭后就再也看不到了

#### 获取 OpenAI API Key

1. 访问 [OpenAI Platform](https://platform.openai.com/)
2. 注册或登录OpenAI账号
3. 点击右上角头像 →「**View API keys**」
4. 点击「**Create new secret key**」，输入名称
5. **立即复制并保存密钥**

> **注意：** OpenAI需要海外手机号验证，且需要绑定信用卡或预充值才能使用。如果你没有海外支付方式，建议选择DeepSeek或通义千问。

#### 获取通义千问 (Qwen) API Key

1. 访问 [阿里云](https://www.aliyun.com/) 注册账号并完成实名认证（需要身份证信息）
2. 访问 [DashScope灵积平台](https://dashscope.console.aliyun.com/overview)，点击「去开通」
3. 开通后访问 [API-KEY管理页面](https://dashscope.console.aliyun.com/apiKey)
4. 点击「**创建新的API-KEY**」
5. **立即复制并保存密钥**——关闭后无法再次查看

> **好消息：** 首次开通赠送100万tokens免费额度（有效期30天）。

#### 获取智谱GLM API Key

1. 访问 [智谱开放平台](https://open.bigmodel.cn/)
2. 用手机号注册账号
3. 登录后进入控制台，点击「**API Keys**」
4. 创建新Key并**立即复制保存**

> **好消息：** GLM-4-Flash模型完全免费，适合入门学习和测试。

**填写到平台：**

将你复制好的API Key粘贴到「**API Key**」输入框中。框默认是密码模式（显示为小黑点），可以点击右侧的眼睛图标查看已输入的内容。

**编辑引擎时的注意事项：**
- 编辑已有引擎时，如果**不需要修改Key**，请保持此框为空（留空表示不修改）
- 编辑时框上方会显示当前Key的脱敏版本（如 `sk-****xxxx`），帮你确认当前使用的是哪个Key

### 4. 默认模型

**这是什么？**
每家AI服务商都提供多个不同能力的模型。你需要指定使用哪个具体模型来处理翻译和摘要任务。

**如何填写？**

当你选择了供应商后，**会自动填入该供应商的推荐模型**：

| 供应商 | 自动填入的模型 | 模型特点 |
|--------|--------------|----------|
| DeepSeek | `deepseek-v4-flash` | 速度快、价格低，适合日常翻译 |
| OpenAI | `gpt-4o-mini` | 轻量高效，性价比高 |
| Qwen (通义千问) | `qwen-plus` | 性能均衡，中文效果好 |
| GLM (智谱) | `glm-4-flash` | 免费使用，适合入门 |

**如果你想使用其他模型，可以手动修改。** 各供应商的常用模型如下：

#### DeepSeek 可用模型

| 模型名称 | 特点 | 推荐场景 |
|----------|------|----------|
| `deepseek-v4-flash` | 速度最快，成本最低 | 日常翻译、摘要生成（推荐） |
| `deepseek-v4-pro` | 推理能力最强 | 论文深度分析、复杂问题 |

#### OpenAI 可用模型

| 模型名称 | 特点 | 推荐场景 |
|----------|------|----------|
| `gpt-4o-mini` | 轻量高效 | 常规翻译和摘要（推荐） |
| `gpt-4o` | 综合能力最强 | 需要高精度的翻译任务 |
| `gpt-4.1` | 最新模型，编程能力强 | 技术文献翻译 |

#### 通义千问 (Qwen) 可用模型

| 模型名称 | 特点 | 推荐场景 |
|----------|------|----------|
| `qwen-turbo` | 速度最快，成本最低 | 大批量翻译 |
| `qwen-plus` | 性能均衡 | 日常使用（推荐） |
| `qwen-max` | 能力最强 | 复杂论文翻译 |
| `qwen-long` | 擅长处理超长文本 | 整篇论文一次性翻译 |

#### 智谱GLM 可用模型

| 模型名称 | 特点 | 推荐场景 |
|----------|------|----------|
| `glm-4-flash` | 免费使用 | 入门学习和测试（推荐） |
| `glm-4-plus` | 综合能力强 | 高质量翻译需求 |

**如何选择模型？——简单的原则：**

- 日常翻译文献、生成摘要 → 用**自动填写的推荐模型**即可
- 遇到翻译质量不满意 → 试试同供应商的更强模型（通常名字里带 `pro`、`max`、`plus` 的更好）
- 翻译内容特别长（整篇论文） → 选标注了"长文本"或上下文窗口大的模型
- 预算有限 → DeepSeek的 `flash` 模型和智谱的 `free` 模型最省钱

### 5. 备用模型

**这是什么？**
当你指定的默认模型因为网络波动、服务商限流等原因暂时不可用时，平台会自动尝试使用备用模型。这是**选填项**，不填也没关系。

**如何填写？**

直接在框中输入模型名称，**多个模型用英文逗号 `,` 分隔**。

**填写示例：**

```
qwen-plus,qwen-turbo,gpt-4o-mini
```

**建议：**
- 备用模型最好和默认模型来自**同一家供应商**，因为它们共用同一个API Key
- 不要填太多，一般1-3个就够
- 如果不确定填什么，可以留空

### 6. 设为默认

**这是什么？**
如果你配置了多个AI引擎（比如同时配置了DeepSeek和OpenAI），平台需要知道在翻译时优先使用哪一个。打开这个开关，表示将该引擎设为默认引擎。

**如何使用？**

- **打开开关**：该引擎成为默认引擎。在翻译和摘要功能中，如果没有特意选择其他引擎，系统会优先使用它
- **关闭开关**：该引擎只是可用引擎之一，不会被自动选中

**注意事项：**
- 同一时间只有一个引擎是"默认"的。如果你把新引擎设为默认，原来的默认引擎会自动取消默认状态
- 你可以在引擎列表页随时更改默认引擎——点击任意引擎卡片上的「**设为默认**」按钮即可
- 即使某个引擎不是默认引擎，你在翻译时仍然可以手动选择它

---

## 完整配置示例

以下以配置 **DeepSeek** 引擎为例，展示完整的填写效果：

| 配置项 | 填写内容 | 说明 |
|--------|----------|------|
| 供应商 | DeepSeek（下拉选择） | 选择深度求索 |
| API 地址 | `https://api.deepseek.com` | 选好供应商后自动填入 |
| API Key | `sk-xxxxxxxxxxxxxxxx` | 从DeepSeek开放平台复制粘贴 |
| 默认模型 | `deepseek-v4-flash` | 选好供应商后自动填入 |
| 备用模型 | `deepseek-v4-pro` | （可选）填了一个更强的备用模型 |
| 设为默认 | 打开开关 | 让这个引擎成为首选 |

填写完成后，点击「**测试连接**」按钮验证配置是否正确，然后点击「**保存**」即可。

> **截图提示：** 截取填写完整的配置对话框，标注6个配置项的位置。在API Key区域建议用马赛克遮挡密钥内容。

---

## 测试连接

保存引擎后，你可以随时点击引擎卡片上的「**编辑**」按钮，然后在编辑对话框中点击「**测试连接**」来验证配置是否正确。

**测试成功时会显示：**
- 绿色的"连接成功"提示
- 该引擎当前可用的所有模型列表（以标签形式展示）

**测试失败时会显示：**
- 红色的错误提示信息

**常见失败原因及解决办法：**

| 错误提示 | 可能原因 | 解决办法 |
|----------|----------|----------|
| 401 或"认证失败" | API Key填写错误 | 检查Key是否完整复制，确保没有多余空格；确认Key没有过期 |
| 404 或"找不到" | API地址填写错误，或模型名称不存在 | 检查API地址是否正确；确认填写的模型名称在服务商处确实存在 |
| 429 或"请求过多" | 短时间内调用次数超限 | 等待几分钟后重试 |
| "连接超时" | 网络问题或地址不可达 | 检查网络连接；确认API地址能否在浏览器中打开 |
| "模型不可用" | 填写的模型名称错误 | 参考上面的模型名称表格，确认拼写完全正确 |

**注意：** 新创建的引擎需要**先保存才能测试**。如果还没保存就点击测试，系统会提示"请先保存引擎后再测试连接"。

---

## 管理已配置的引擎

在引擎列表页面，每张卡片展示了以下信息：

| 展示内容 | 说明 |
|----------|------|
| 供应商名称 | 如"DeepSeek"、"OpenAI" |
| 模型 | 当前使用的默认模型名称 |
| API 地址 | 请求发送的目标地址 |
| Key | 脱敏显示，如 `sk-****xxxx`，保护你的密钥安全 |
| 备用模型 | 如果填写了备用模型，会显示在这里 |
| 默认标签 | 绿色标签，表示该引擎是当前默认引擎 |

每张卡片右侧有4个操作按钮：

| 按钮 | 功能 |
|------|------|
| 设为默认 | 将该引擎设为系统默认引擎 |
| 编辑 | 修改引擎配置信息 |
| 删除 | 移除该引擎配置 |

---

## 常见问题

**Q：我可以配置多个引擎吗？**

A：可以。比如你可以同时配置DeepSeek（用于日常翻译）和OpenAI（用于重要论文的高质量翻译）。在翻译页面可以自由切换。

**Q：API Key是什么？从哪里获取？**

A：API Key是你在AI服务商处注册后获得的密钥。你需要先去服务商官网注册账号，然后在"API Keys"或"密钥管理"页面创建。具体步骤见上面[第3节"API Key"](#3-api-key)中各供应商的分步说明。

**Q：API Key需要付费吗？**

A：大多数服务商采用按量付费模式。好消息是：
- DeepSeek新用户赠送500万tokens免费额度
- 通义千问首次开通赠送100万tokens（30天有效）
- 智谱GLM-4-Flash完全免费
- OpenAI需要预充值或绑定信用卡

建议先使用有免费额度的服务商（DeepSeek或智谱）进行体验。

**Q：我不是程序员，不太懂Token、上下文窗口这些概念，有影响吗？**

A：完全不影响。你只需要按照上面的步骤，选择供应商、填好Key、用自动填写的默认模型即可。平台已经帮你做好了合理配置，直接使用就好。

**Q：编辑引擎时API Key框显示"留空则不修改"是什么意思？**

A：当你修改已有引擎的配置时，如果你只是改模型或者其他信息，不需要重新输入Key，直接留空就行。只有想更换Key时才需要填入新的。

**Q：如何知道我的配置是否正确？**

A：保存引擎后，进入编辑对话框，点击「测试连接」按钮。如果显示绿色"连接成功"并列出可用模型，说明配置正确。

**Q：平台会不会泄露我的API Key？**

A：你的API Key存储在平台的服务器上，经过加密保存。在界面中显示时也会自动脱敏（只显示前几位和最后几位）。但请勿将API Key通过截图、聊天等方式分享给他人。

---

> **文档版本：** 1.0
>
> **最后更新：** 2026年5月18日
>
> **适用平台：** InkLight 智能文献阅读与管理平台