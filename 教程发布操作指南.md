# 教程发布操作指南

> 本指南面向普通用户，无需任何技术背景。按照以下步骤，你可以在平台上轻松创建和发布图文并茂的使用教程。

---

## 一、认识发布工作台

发布教程需要使用后台的「使用教程管理」页面。这个页面的入口通常位于系统管理菜单下，需要管理员权限才能访问。

进入页面后，你会看到：

| 界面元素 | 说明 |
|----------|------|
| **「新建教程」按钮** | 页面右上角的蓝色按钮，点击后开始创建新教程 |
| **教程列表** | 已创建的教程会以表格形式展示，可看到标题、发布状态、更新时间 |
| **操作按钮** | 每条教程右侧有"编辑""版本""发布/取消发布""删除"四个按钮 |

> **截图获取提示：** 可在教程管理列表页截取全屏界面。重点展示"新建教程"按钮和教程列表的布局结构。

---

## 二、新建教程：各区域填写指南

点击「新建教程」按钮后，会弹出一个编辑窗口，包含**三个填写区域**：标题、摘要、内容。

### 2.1 标题区域

- **位置：** 弹窗最上方，一个白色的文本输入框
- **标签提示：** 框左边有"标题"两个字，后面带红色星号，表示必填
- **填写规则：** 输入教程的名称，例如"DeepSeek API 配置教程"
- **字数限制：** 最多200个字，框右下角会实时显示已输入字数
- **建议：** 标题要简洁明了，让读者一眼就知道这篇教程讲什么

| 好标题示例 | 不好标题示例 |
|------------|-------------|
| DeepSeek API 注册与密钥获取指南 | 关于DeepSeek这个AI模型的一些API接口使用方面的心得分享与配置步骤详解（太长） |
| 通义千问 Qwen 翻译服务接入教程 | API（太短，看不出内容） |

> **截图获取提示：** 聚焦弹窗中的标题输入框，展示"标题"标签的红色星号和字数计数器。

### 2.2 摘要区域

- **位置：** 标题输入框下方，一个较高的灰色文本区域
- **标签提示：** 框左边有"摘要"两个字，不带星号，表示选填
- **填写规则：** 用一两句话概括这篇教程的内容和适用人群
- **字数限制：** 最多500个字
- **发布后效果：** 读者在教程列表页就能看到这段摘要，决定是否点进去阅读

填写示例：

```
本文详细介绍如何注册 DeepSeek 开放平台账号、创建 API 密钥，
并提供 Python 和 JavaScript 两种语言的调用示例。适合首次接触
大语言模型 API 的开发者阅读。
```

> **截图获取提示：** 聚焦摘要输入框，展示已填写的摘要内容和字数计数。

### 2.3 内容区域（富文本编辑器）

这是最重要的区域，也是你编辑教程正文的地方。

- **位置：** 摘要输入框下方，一个带有工具栏的白色编辑区域
- **标签提示：** 框左边有"内容"两个字
- **占位提示：** 编辑区域内部灰色字显示"开始编写教程内容..."

#### 工具栏按钮说明

工具栏在编辑器顶部，按钮按功能分为六组：

**第一组：文字样式按钮**

| 按钮 | 外观 | 功能 | 使用方式 |
|------|------|------|----------|
| **B** | 粗体字母B | 将文字变粗 | 选中文字→点击B按钮 |
| *I* | 斜体字母I | 将文字变斜 | 选中文字→点击I按钮 |
| U | 带下划线的U | 给文字加下划线 | 选中文字→点击U按钮 |
| S | 带删除线的S | 给文字加删除线 | 选中文字→点击S按钮 |

**第二组：标题级别按钮**

| 按钮 | 外观 | 功能 | 使用场景 |
|------|------|------|----------|
| H1 | 显示"H1" | 一级标题，字最大 | 教程主标题、大章节标题 |
| H2 | 显示"H2" | 二级标题，字中等 | 每个主要部分的标题（如"注册步骤"） |
| H3 | 显示"H3" | 三级标题，字较小 | 子章节标题（如"步骤一：访问官网"） |

**标题使用建议：**
- 一篇教程建议只用**一个一级标题**（就是标题栏填的那个）
- 在正文中，用**二级标题**划分大段，用**三级标题**划分小段
- 不要让文字看起来"跳跃"太大——比如从一级标题直接跳到普通正文

**第三组：对齐方式按钮**

| 按钮 | 外观 | 功能 |
|------|------|------|
| 左对齐 | 显示"左对齐" | 文字靠左排列（默认，最常用） |
| 居中 | 显示"居中" | 文字居中排列（适合小标题或引用） |
| 右对齐 | 显示"右对齐" | 文字靠右排列（较少使用） |

**第四组：列表按钮**

| 按钮 | 外观 | 功能 | 使用场景 |
|------|------|------|----------|
| 无序列表 | 带圆点图标 | 创建项目符号列表 | 并列的内容，如功能清单、注意事项 |
| 有序列表 | 带数字图标 | 创建编号列表 | 有先后顺序的步骤说明 |

**第五组：插入按钮**

| 按钮 | 外观 | 功能 |
|------|------|------|
| 链接 | 链条图标 | 给文字添加可点击的超链接 |
| 图片 | 图片图标 | 从本地电脑上传图片并插入正文 |

**添加超链接的步骤：**
1. 选中你想变成链接的文字（比如"DeepSeek官网"）
2. 点击链条图标按钮
3. 在弹出的对话框中输入完整的网址（必须以 `https://` 开头）
4. 点击"确定"
5. 成功后，文字会变成蓝色并带有下划线

**插入图片的步骤：**
1. 将光标放在你想插入图片的位置
2. 点击图片图标按钮
3. 在弹出的文件选择器中，找到你电脑上的图片并选中
4. 确认上传
5. 等待图片上传完成（通常几秒钟），图片会自动出现在光标位置

**图片上传要求：**

| 要求 | 说明 |
|------|------|
| 文件格式 | 仅支持 `.jpg`、`.jpeg`、`.png`、`.gif`、`.webp` 格式 |
| 文件大小 | 单张图片不超过 **5MB** |
| 建议尺寸 | 宽度建议控制在 800~1200 像素之间，便于阅读 |

> **截图获取提示：** 录制一段10秒的录屏，展示从点击图片按钮→选择本地文件→图片出现在编辑器中的完整流程。同时截图展示链接输入对话框。

---

## 三、正文内容编写建议

### 3.1 内容组织格式

基于「大模型与翻译API配置完整教程」的结构，建议按以下格式组织正文：

**开头部分：** 用简短段落说明本教程的用途

> 示例："本教程介绍如何注册 DeepSeek 开放平台账号，获取 API 密钥，并使用 Python 进行首次调用。"

**正文部分：** 使用二级标题分隔不同的小节

> 示例结构：
> - ## 注册账号（二级标题）
> - ### 步骤一：访问官网（三级标题）
>   - 具体说明...
> - ### 步骤二：填写信息（三级标题）
>   - 具体说明...
> - ## 获取API密钥（二级标题）
> - ## 首次调用测试（二级标题）

**使用列表增强可读性：**

对于有步骤的内容，使用**有序列表**（带数字的图标按钮）：
- 1. 打开浏览器
- 2. 访问官方网址
- 3. 点击右上角注册按钮

对于并列的注意事项，使用**无序列表**（带圆点的图标按钮）：
- 注意：密钥仅显示一次，请立即保存
- 注意：不要将密钥分享给他人
- 提醒：建议开启两步验证

### 3.2 图片位置建议

| 教程位置 | 建议插入的图片 |
|----------|---------------|
| 注册流程章节 | 官网首页截图、注册页截图 |
| 密钥获取章节 | 控制台页面截图、密钥创建对话框 |
| 计费说明章节 | 价格页面截图（如有） |
| 错误排查章节 | 错误提示截图 |

> **截图标记技巧：** 在截图中用红框或箭头标注关键按钮和输入位置，帮助读者快速定位。可以使用微信截图工具（快捷键 `Alt+A`）或系统截图工具中的标记功能。

---

## 四、保存与发布流程

### 4.1 保存为草稿

1. 编辑完成后，点击编辑窗口右下角的「保存」按钮
2. 系统会提示"教程已创建"或"教程已更新"
3. 教程会出现在列表中，状态标签显示为灰色的"草稿"
4. 草稿只有管理员可见，普通用户看不到

### 4.2 发布教程

1. 在教程列表中找到你要发布的教程
2. 点击右侧操作栏中的「发布」按钮（绿色）
3. 状态标签会变为绿色的"已发布"
4. 读者现在可以在教程页看到这篇内容了

### 4.3 取消发布

如果教程需要临时下架：

1. 点击已发布教程右侧的「取消发布」按钮（橙色）
2. 教程恢复为草稿状态，读者不再能看到

### 4.4 修改已发布的内容

1. 点击教程右侧的「编辑」按钮
2. 修改标题、摘要或正文内容
3. 点击「保存」
4. 系统会自动生成一个新版本，同时【旧内容也会自动更新到已发布页面】
5. 如果改错了，可以通过"版本"按钮恢复到之前的版本

---

## 五、版本管理

每次保存教程时，系统会自动保存一个版本快照。你可以随时查看和恢复历史版本。

### 查看版本历史

1. 在教程列表中找到目标教程
2. 点击右侧的「版本」按钮
3. 会弹出版本历史窗口，按时间倒序显示所有版本
4. 每个版本显示：版本编号、创建时间

### 恢复旧版本

1. 在版本历史窗口中，找到你想恢复的版本
2. 点击该版本下的「预览」按钮，先看看内容是否正确
3. 确认无误后，点击「恢复此版本」
4. 系统会提示"版本已恢复"，当前内容会被替换为该版本的内容

> **注意：** 恢复版本同样会创建新的版本记录，不会丢失当前正在使用的内容。

---

## 六、常见问题

**Q：插入的图片在编辑器中显示正常，但发布后不显示怎么办？**

A：检查图片文件大小是否超过5MB，格式是否为支持的格式（jpg/png/gif/webp）。尝试重新上传。

**Q：如何调整图片在文中的大小？**

A：当前编辑器暂不支持直接拖拽调整图片大小。建议在上传前用图片工具将图片调整为合适尺寸（宽度800~1200像素为佳）。

**Q：教程发布后，还能修改吗？**

A：可以。修改后的内容会自动更新到已发布页面，无需重新发布。

**Q：忘记了之前写的内容，怎么办？**

A：使用「版本」功能查看历史版本，可以恢复到任意之前的版本。

**Q：标题栏和正文字号有什么关系？**

A：标题栏的内容会作为教程的大标题显示在页面上方，正文字号在编辑器中通过H1/H2/H3按钮控制。建议不要在正文中使用H1（一级标题），因为它和标题栏的字号相同，会造成视觉混淆。

---

> **文档版本：** 1.0
>
> **最后更新：** 2026年5月18日
>
> **适用平台：** InkLight 智能文献阅读与管理平台