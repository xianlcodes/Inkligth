# InkLight（研墨）— 智能文献阅读与管理平台

## 1. 项目概述

### 1.1 项目背景

研究生和科研人员在日常工作中需要大量阅读、管理和分析学术论文。传统的文献管理工具（如 Zotero、Mendeley）侧重文献书目管理，而 PDF 阅读器则缺乏智能分析和协作能力。InkLight 旨在弥补这一空白——将 AI 驱动的分析能力（翻译、摘要、创新点提取、组会 PPT 生成）与流畅的 PDF 阅读体验深度融合。

### 1.2 核心价值

| 维度 | 价值 |
|------|------|
| **效率提升** | AI 自动翻译、提取摘要和创新点，省去手动整理时间 |
| **沉浸阅读** | 高亮标注、笔记系统与 PDF 阅读无缝集成，不打断阅读流 |
| **学术场景闭环** | 从文献上传 → 阅读批注 → AI 分析 → 组会 PPT 大纲，全链路覆盖 |
| **个性配置** | 用户可配置多种 AI 引擎（OpenAI / 兼容 API），按需切换模型 |

### 1.3 目标用户

- 研究生：日常文献精读、组会汇报准备
- 科研人员：文献综述、创新点追踪
- 课题组：团队文献共享与讨论

---

## 2. 技术栈

### 2.1 前端

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue 3 | ^3.5 | UI 框架（Composition API） |
| TypeScript | ~6.0 | 类型安全 |
| Vite | ^8.0 | 构建工具 |
| Pinia | ^3.0 | 状态管理 |
| Vue Router | ^4.6 | 路由 |
| Element Plus | ^2.13 | UI 组件库 |
| Axios | ^1.16 | HTTP 客户端 |
| vue-pdf-embed | ^2.1 | PDF 渲染（封装 pdf.js） |
| Tiptap | ^3.23 | 富文本编辑器 |
| ECharts | （集成） | 统计图表 |

### 2.2 后端

| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.11+ | 运行时 |
| FastAPI | 0.115 | Web 框架 |
| SQLAlchemy | 2.0（异步） | ORM |
| asyncpg | 0.30 | PostgreSQL 异步驱动 |
| Pydantic | v2 | 数据验证 |
| Celery | — | 异步任务队列 |
| Redis | 5.2 | 缓存 & 消息代理 |
| PyMuPDF | 1.24 | PDF 文本提取 |
| python-jose | 3.5 | JWT 令牌 |
| passlib + bcrypt | — | 密码哈希 |
| OpenAI SDK | 1.54 | AI 调用 |
| python-pptx | 1.0 | PPT 生成 |

### 2.3 数据库与存储

| 技术 | 用途 |
|------|------|
| PostgreSQL | 主数据库 |
| pgvector | 向量嵌入存储与语义搜索 |
| MinIO | （可选）对象存储 |
| Redis | 会话缓存 & 任务队列 |

---

## 3. 项目结构

```
code/
├── backend/                          # 后端（Python FastAPI）
│   ├── app/
│   │   ├── api/                      # 路由（按资源分文件）
│   │   │   ├── auth.py               # 认证（登录/注册/刷新/登出）
│   │   │   ├── literature.py         # 文献（上传/列表/详情/更新/删除）
│   │   │   ├── translate.py          # 翻译（逐段/流式/全文）
│   │   │   ├── note.py               # 笔记 & 高亮标注
│   │   │   ├── tag.py                # 标签系统
│   │   │   ├── folder.py             # 文件夹管理
│   │   │   ├── search.py             # 语义搜索（RAG）
│   │   │   ├── ai_engine.py          # AI 引擎配置
│   │   │   ├── announcement.py       # 公告通知
│   │   │   ├── presentation.py       # 组会 PPT 大纲
│   │   │   ├── stats.py              # 阅读统计
│   │   │   ├── tasks.py              # 任务状态轮询
│   │   │   ├── users.py              # 用户信息
│   │   │   └── health.py             # 健康检查
│   │   ├── core/                     # 核心配置与基础设施
│   │   │   ├── config.py             # 配置管理（pydantic-settings）
│   │   │   ├── security.py           # JWT + bcrypt + refresh token 生成
│   │   │   ├── deps.py               # 依赖注入（get_current_user）
│   │   │   ├── ai_client.py          # AI 客户端工厂（按引擎配置创建）
│   │   │   └── ai_providers/         # AI 引擎抽象层
│   │   │       ├── translator.py     # 翻译 Provider（OpenAI）
│   │   │       ├── analyzer.py       # 分析 Provider
│   │   │       └── outline_generator.py  # PPT 大纲 Provider
│   │   ├── db/
│   │   │   └── database.py           # 数据库引擎 & 会话工厂
│   │   ├── models/                   # SQLAlchemy ORM 模型
│   │   │   ├── user.py               # 用户
│   │   │   ├── literature.py         # 文献
│   │   │   ├── note.py               # 笔记（含高亮坐标）
│   │   │   ├── tag.py                # 标签
│   │   │   ├── folder.py             # 文件夹
│   │   │   ├── ai_engine.py          # AI 引擎
│   │   │   ├── ai_analysis.py        # AI 分析结果
│   │   │   ├── literature_chunk.py   # 文献分段（用于 RAG）
│   │   │   ├── reading_record.py     # 阅读记录
│   │   │   ├── presentation.py       # PPT 大纲
│   │   │   ├── announcement.py       # 公告
│   │   │   └── refresh_token.py      # Refresh Token
│   │   ├── schemas/                  # Pydantic 请求/响应模型
│   │   │   ├── user.py
│   │   │   ├── literature.py
│   │   │   ├── note.py
│   │   │   ├── tag.py
│   │   │   ├── folder.py
│   │   │   ├── ai_engine.py
│   │   │   ├── analysis.py
│   │   │   ├── announcement.py
│   │   │   ├── presentation.py
│   │   │   ├── refresh_token.py
│   │   │   ├── stats.py
│   │   │   └── health.py
│   │   ├── services/                 # 业务逻辑层
│   │   │   ├── user_service.py       # 用户服务
│   │   │   ├── literature_service.py # 文献服务（含 PDF 解析、元数据提取）
│   │   │   ├── note_service.py       # 笔记服务
│   │   │   ├── tag_service.py        # 标签服务
│   │   │   ├── folder_service.py     # 文件夹服务
│   │   │   ├── search_service.py     # 语义搜索 & 向量索引
│   │   │   ├── ai_engine_service.py  # AI 引擎管理（含 API Key 加解密）
│   │   │   ├── analysis_service.py   # 分析结果存储
│   │   │   ├── announcement_service.py # 公告服务
│   │   │   ├── presentation_service.py # PPT 大纲服务
│   │   │   ├── stats_service.py      # 统计数据
│   │   │   ├── refresh_token_service.py # Refresh Token 服务
│   │   │   ├── pptx_service.py       # PPTX 文件生成
│   │   │   ├── embedding_service.py  # 向量嵌入服务
│   │   │   └── health_service.py     # 健康检查
│   │   └── utils/
│   │       └── task_store.py         # 异步任务状态追踪（内存存储）
│   ├── alembic/                      # 数据库迁移
│   ├── tests/
│   ├── main.py                       # FastAPI 入口
│   ├── worker.py                     # Celery Worker 入口
│   ├── init_db.py                    # 数据库初始化脚本
│   ├── requirements.txt
│   └── .env                          # 环境变量
│
├── inklight-frontend/                # 前端（Vue 3 + TypeScript）
│   ├── src/
│   │   ├── api/                      # API 封装（按模块分文件）
│   │   │   ├── client.ts             # Axios 实例 + JWT 拦截器 + 静默刷新
│   │   │   ├── literature.ts
│   │   │   ├── note.ts
│   │   │   ├── tag.ts
│   │   │   ├── folder.ts
│   │   │   ├── translate.ts
│   │   │   ├── analysis.ts
│   │   │   ├── outline.ts
│   │   │   ├── search.ts
│   │   │   ├── aiEngine.ts
│   │   │   ├── announcement.ts
│   │   │   ├── presentation.ts
│   │   │   └── stats.ts
│   │   ├── assets/                   # 静态资源
│   │   ├── composables/              # 组合式函数（useXxx）
│   │   ├── router/
│   │   │   └── index.ts              # 路由配置 & 导航守卫
│   │   ├── stores/                   # Pinia 状态管理
│   │   │   ├── auth.ts               # 用户认证
│   │   │   ├── literature.ts         # 文献列表状态
│   │   │   └── aiEngine.ts           # AI 引擎状态
│   │   ├── styles/
│   │   │   └── theme.css             # 全局样式 & CSS 变量
│   │   ├── types/                    # TypeScript 类型声明
│   │   ├── utils/                    # 工具函数
│   │   ├── views/                    # 页面组件
│   │   │   ├── auth/
│   │   │   │   └── Login.vue         # 登录/注册页
│   │   │   ├── literature/
│   │   │   │   ├── LiteratureList.vue # 文献列表（含文件夹侧边栏）
│   │   │   │   └── Reader.vue        # PDF 阅读器（核心页面）
│   │   │   ├── notes/
│   │   │   │   └── NoteList.vue      # 笔记列表
│   │   │   ├── presentation/
│   │   │   │   └── PreMeeting.vue    # 组会看板
│   │   │   ├── settings/
│   │   │   │   └── AiEngineSettings.vue # AI 引擎配置
│   │   │   ├── stats/
│   │   │   │   └── ReadingCalendar.vue # 阅读日历
│   │   │   └── announcements/
│   │   │       └── AnnouncementList.vue # 公告列表
│   │   ├── App.vue                   # 根组件（布局、导航、公告栏）
│   │   ├── main.ts                   # 入口（注入 Router 到 API Client）
│   │   └── style.css
│   ├── package.json
│   ├── tsconfig.json
│   └── vite.config.ts
│
├── .trae/rules/                      # AI 编码助手规范
└── PROJECT.md                        # 本文档
```

---

## 4. 数据库设计

### 4.1 表结构概览

共 **12 张表** + 1 张多对多关联表：

| 表名 | 模型 | 说明 |
|------|------|------|
| `users` | [User](backend/app/models/user.py) | 用户（id, email, hashed_password, username, is_admin） |
| `literatures` | [Literature](backend/app/models/literature.py) | 文献（title, authors, abstract, doi, raw_text, translated_text, folder_id, status） |
| `folders` | [Folder](backend/app/models/folder.py) | 文件夹（name, parent_id, user_id） |
| `notes` | [Note](backend/app/models/note.py) | 笔记/高亮标注（page_number, rect_coords, quoted_text, content, note_type） |
| `tags` | [Tag](backend/app/models/tag.py) | 标签（name, user_id） |
| `literature_tags` | 关联表 | 文献-标签多对多 |
| `ai_engines` | [AIEngine](backend/app/models/ai_engine.py) | AI 引擎配置（provider, api_base, api_key, default_model） |
| `ai_analyses` | [AIAnalysis](backend/app/models/ai_analysis.py) | AI 分析结果（summary, innovations, methods） |
| `literature_chunks` | [LiteratureChunk](backend/app/models/literature_chunk.py) | 文献分段（chunk_index, chunk_text, embedding） |
| `reading_records` | [ReadingRecord](backend/app/models/reading_record.py) | 阅读记录（record_date, pages_read, reading_time_seconds） |
| `presentations` | [Presentation](backend/app/models/presentation.py) | PPT 大纲（slides JSON） |
| `announcements` | [Announcement](backend/app/models/announcement.py) | 系统公告（title, content, level, is_pinned, expires_at） |
| `refresh_tokens` | [RefreshToken](backend/app/models/refresh_token.py) | Refresh Token（token, user_id, expires_at） |

### 4.2 核心关系 ER

```
User (1) ──< (N) Literature ──< (N) LiteratureChunk
 ├── (1) ──< (N) Note
 ├── (1) ──< (N) Folder ──< (N) Literature (可选归属)
 ├── (1) ──< (N) AIAnalysis (per literature, unique)
 ├── (1) ──< (N) AIEngine
 ├── (1) ──< (N) Tag <──> Literature (多对多)
 ├── (1) ──< (N) ReadingRecord
 ├── (1) ──< (N) Presentation
 └── (1) ──< (N) RefreshToken
```

### 4.3 设计原则

- **主键**：统一使用 UUID 字符串（`String` + `uuid.uuid4()`），便于分布式扩展
- **时间戳**：所有表含 `created_at`，可更新表含 `updated_at`
- **外键**：严格定义，使用 `ON DELETE CASCADE` 或 `SET NULL` 按需选择
- **软删除**：当前未启用，可后续扩展 `is_deleted` 字段
- **向量字段**：`literature_chunks.embedding` 使用 `ARRAY(Float)`，后续可迁移到 pgvector 的 `Vector` 类型

---

## 5. 主要功能模块

### 5.1 用户认证与授权

```
┌──────────────────────────────────────┐
│          认证流程                     │
│                                      │
│  登录 → POST /auth/token              │
│      ← access_token (JWT, 60min)      │
│      ← refresh_token (opaque, 7d)     │
│                                      │
│  访问 → Authorization: Bearer <JWT>  │
│                                      │
│  JWT 过期 → 401                      │
│    → 拦截器自动 POST /auth/refresh   │
│    → 获取新 JWT（用户无感知）         │
│                                      │
│  Refresh Token 也过期 → 跳转登录页    │
│                                      │
│  登出 → POST /auth/logout            │
│     → 服务端销毁 Refresh Token       │
└──────────────────────────────────────┘
```

**技术要点**：
- Access Token：JWT（HS256 签名），**60 分钟**过期，承载 `sub`（user_id）
- Refresh Token：64 字符随机 hex，**7 天**过期，存储在数据库可吊销
- **令牌轮换**：每次刷新同时签发新 Refresh Token 并作废旧令牌
- **并发安全**：前端拦截器维护 `isRefreshing` 锁 + `pendingRequests` 队列，确保多个并发 401 只触发一次刷新

### 5.2 文献上传与管理

**上传流程**：
1. 前端上传 PDF → 后端保存文件并提取原始文本（PyMuPDF）
2. AI 提取元数据（标题、作者、摘要、年份、期刊、DOI）
3. DOI 去重检查
4. 文本分段并异步建立向量索引（用于后续 RAG 搜索）

**文献管理**：
- 按文件夹分类（支持创建/重命名/删除文件夹，文献可移动）
- 按标签分类（多对多标签系统）
- 阅读状态追踪（未读/阅读中/已读）

### 5.3 PDF 阅读器（核心页面）

`Reader.vue` — 项目中最复杂的页面（~2550 行），集成：

| 功能 | 实现方式 |
|------|----------|
| **PDF 渲染** | vue-pdf-embed（封装 pdf.js），Canvas 渲染，支持缩放（0.5x-3x）和适应宽度 |
| **高亮标注** | 在 PDF 文本层之上添加彩色覆盖层，坐标基于页面百分比（0-1） |
| **浮动菜单** | 选中文本弹出"高亮/记笔记/翻译"按钮；点击高亮弹出"编辑笔记/删除高亮"按钮 |
| **段落模式** | 可切换逐段模式，点击段落直接翻译 |
| **面板拖拽** | PDF 面板与侧边栏之间可拖拽调整宽度 |
| **侧边栏折叠** | 左侧导航栏折叠后 PDF 区域自动扩展（防抖 350ms 避免 PDF 渲染冲突） |
| **页面检测** | 通过 Intersection 算法检测当前阅读页码（30 秒上报阅读记录） |
| **搜索跳转** | 支持通过搜索结果跳转到 PDF 对应段落并高亮 |

### 5.4 智能翻译

**逐段翻译**（`POST /translate/text`）：
- 点击 PDF 段落 → 选中文本 → 调用翻译 API
- 支持**连续翻译模式**：连续点击多个段落，译文自动追加到列表中

**流式翻译**（`POST /translate/text/stream`）：
- 使用 SSE（Server-Sent Events）实现打字机效果
- 前端 EventSource 接收流式数据

**全文翻译**（`POST /literatures/{id}/translate/full`）：
- 异步后台任务（BackgroundTasks），将文本按段落拆分
- 每段独立调用 AI 翻译（可超时重试 3 次，`timeout=120s`）
- 翻译进度通过轮询 `GET /tasks/{id}/status` 查看
- 进度对话框支持"关闭→后台翻译"继续操作
- 译文与原文在侧边栏中对照显示，支持点击跳转

**翻译提示词**：
```
Translate the following text from en to zh.
Only return the translated text, no explanations:
{text}
```

### 5.5 AI 文献分析

通过 `POST /literatures/{id}/analyze` 启动异步分析：

AI 分析提示词（`ANALYSIS_PROMPT`）要求返回 JSON：
```json
{
  "summary": {
    "background": "研究背景与动机",
    "method": "核心方法",
    "result": "关键发现",
    "conclusion": "主要结论"
  },
  "innovations": ["创新点1", "创新点2", ...],
  "methods": "可复现的方法步骤"
}
```

创新点可一键"添加为笔记"。

### 5.6 组会 PPT 大纲生成

- 调用 AI 生成 **9 页** 标准组会汇报大纲（标题页→研究背景→核心问题→方法→实验→结果→创新→局限→总结）
- 大纲存储为 JSON（slides 数组），可查看/下载/重新生成
- 支持导出 PPTX 文件（python-pptx 生成）
- 同步到"组会看板"页面管理

### 5.7 笔记系统

- 每篇笔记关联到文献、页码、**矩形坐标**（`{x, y, width, height}`，0-1 范围）
- `note_type`：`general`（通用笔记）、`highlight`（高亮）、`innovation`（创新点）
- 高亮渲染：在 PDF 文本层上根据 `rect_coords` 和 `page_number` 绘制彩色覆盖层
- 删除笔记时自动移除对应高亮

### 5.8 语义搜索

- 文献上传后异步将文本分块（`LiteratureChunk`）并索引
- 搜索时调用 embedding 模型（`all-MiniLM-L6-v2`）向量化查询
- 通过向量相似度匹配返回最相关段落
- 搜索结果可跳转到 PDF 原文位置

### 5.9 公告通知

- 管理员可发布/编辑/删除公告（`is_admin` 鉴权）
- 公告支持等级（info/warning/success）、置顶、定时发布、过期
- 全站顶部公告栏展示最新 3 条有效公告
- 用户可逐条关闭公告（状态存储于 localStorage）

### 5.10 阅读统计

- 每 30 秒上报阅读记录（当前页码 + 阅读时长）
- 阅读日历页面展示每日阅读时长和页数
- 文献详情页显示累积阅读时间

---

## 6. API 接口汇总

### 6.1 接口总览

基础路径：`/api/v1`

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| `GET` | `/health` | 无 | 健康检查 |
| `POST` | `/auth/register` | 无 | 注册 |
| `POST` | `/auth/token` | 无 | 登录 |
| `POST` | `/auth/refresh` | 无 | 刷新 Token |
| `POST` | `/auth/logout` | Bearer | 登出 |
| `GET` | `/users/me` | Bearer | 当前用户信息 |
| `GET` | `/literatures` | Bearer | 文献列表 |
| `POST` | `/literatures` | Bearer | 上传文献 |
| `GET` | `/literatures/{id}` | Bearer | 文献详情 |
| `PATCH` | `/literatures/{id}` | Bearer | 更新文献 |
| `DELETE` | `/literatures/{id}` | Bearer | 删除文献 |
| `GET` | `/literatures/{id}/file` | Bearer | 下载 PDF |
| `POST` | `/literatures/{id}/translate/full` | Bearer | 启动全文翻译 |
| `POST` | `/literatures/{id}/analyze` | Bearer | 启动 AI 分析 |
| `GET` | `/literatures/{id}/analysis` | Bearer | 获取分析结果 |
| `POST` | `/literatures/{id}/presentation-outline` | Bearer | 生成 PPT 大纲 |
| `GET` | `/literatures/{id}/presentation-outline/pptx` | Bearer | 下载 PPTX |
| `POST` | `/translate/text` | Bearer | 逐段翻译 |
| `POST` | `/translate/text/stream` | Bearer | 流式翻译 (SSE) |
| `GET` | `/tasks/{task_id}/status` | Bearer | 任务状态轮询 |
| `GET` | `/notes` | Bearer | 笔记列表 |
| `POST` | `/notes` | Bearer | 创建笔记 |
| `PATCH` | `/notes/{id}` | Bearer | 更新笔记 |
| `DELETE` | `/notes/{id}` | Bearer | 删除笔记 |
| `GET` | `/tags` | Bearer | 标签列表 |
| `POST` | `/tags` | Bearer | 创建标签 |
| `PATCH` | `/tags/{id}` | Bearer | 更新标签 |
| `DELETE` | `/tags/{id}` | Bearer | 删除标签 |
| `GET` | `/folders` | Bearer | 文件夹列表 |
| `POST` | `/folders` | Bearer | 创建文件夹 |
| `PATCH` | `/folders/{id}` | Bearer | 重命名文件夹 |
| `DELETE` | `/folders/{id}` | Bearer | 删除文件夹 |
| `POST` | `/search` | Bearer | 语义搜索 |
| `GET` | `/ai-engines` | Bearer | AI 引擎列表 |
| `POST` | `/ai-engines` | Bearer | 添加 AI 引擎 |
| `PATCH` | `/ai-engines/{id}` | Bearer | 编辑 AI 引擎 |
| `DELETE` | `/ai-engines/{id}` | Bearer | 删除 AI 引擎 |
| `POST` | `/ai-engines/{id}/test` | Bearer | 测试连接 |
| `POST` | `/ai-engines/{id}/set-default` | Bearer | 设为默认 |
| `GET` | `/stats/daily` | Bearer | 阅读统计 |
| `POST` | `/stats/reading-records` | Bearer | 上报阅读记录 |
| `GET` | `/announcements/active` | 无 | 有效公告 |
| `GET` | `/announcements` | Bearer | 公告列表 |
| `POST` | `/announcements` | Bearer+Admin | 发布公告 |
| `PATCH` | `/announcements/{id}` | Bearer+Admin | 编辑公告 |
| `DELETE` | `/announcements/{id}` | Bearer+Admin | 删除公告 |
| `POST` | `/presentations` | Bearer | 存储 PPT 大纲 |
| `GET` | `/presentations` | Bearer | PPT 大纲列表 |
| `DELETE` | `/presentations/{id}` | Bearer | 删除 PPT 大纲 |

### 6.2 统一响应格式

```json
{
  "code": 200,
  "msg": "success",
  "data": {}
}
```

分页接口附带：
```json
{
  "total": 100,
  "items": []
}
```

---

## 7. 系统架构

### 7.1 请求链路

```
┌──────────┐     HTTP/SSE      ┌──────────┐     SQL (async)     ┌────────────┐
│  Vue 3   │ ←───────────────→ │ FastAPI  │ ←────────────────→ │ PostgreSQL │
│  前端     │                   │  后端    │                     │   主数据库  │
└──────────┘                   └────┬─────┘                     └────────────┘
                                    │
                          ┌─────────┼─────────┐
                          │         │         │
                    ┌─────┴──┐ ┌───┴───┐ ┌───┴───┐
                    │ Celery │ │ Redis │ │ OpenAI│
                    │ Worker │ │       │ │ API   │
                    └────────┘ └───────┘ └───────┘
```

### 7.2 AI 引擎抽象层

```
                    ┌─────────────────────────┐
                    │     ai_client.py         │
                    │  get_user_ai_client()    │
                    │  get_user_default_model()│
                    └───────────┬─────────────┘
                                │
          ┌─────────────────────┼─────────────────────┐
          │                     │                     │
  ┌───────┴───────┐   ┌────────┴────────┐   ┌────────┴────────┐
  │  Translator   │   │    Analyzer     │   │ OutlineGenerator│
  │  (translator) │   │   (analyzer)    │   │ (outline_gen)   │
  └───────────────┘   └─────────────────┘   └─────────────────┘
          │                     │                     │
          └─────────────────────┼─────────────────────┘
                                │
                    ┌───────────┴───────────┐
                    │    OpenAI AsyncOpenAI  │
                    │    (或兼容 API)        │
                    └───────────────────────┘
```

### 7.3 认证流程（Refresh Token 机制）

```
登录         → 收到 access_token(JWT 60min) + refresh_token(opaque 7d)
               
API 调用     → 请求头带 Bearer access_token
  ├─ 成功     → 正常响应
  └─ 401      → 拦截器自动调用 POST /auth/refresh
                ├─ 成功 → 更新 token，重试原请求（用户无感知）
                └─ 失败 → 清除认证状态 → 跳转登录页
               
登出         → POST /auth/logout → 服务端删 refresh_token
```

---

## 8. 环境配置与部署指南

### 8.1 环境要求

| 依赖 | 最低版本 |
|------|----------|
| Python | 3.11+ |
| Node.js | 18+ |
| PostgreSQL | 14+ |
| Redis | 6+ |

### 8.2 后端配置

**1. 克隆项目并进入后端目录**：
```bash
cd backend
```

**2. 创建虚拟环境并安装依赖**：
```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
```

**3. 配置环境变量（`.env`）**：
```env
DATABASE_URL=postgresql+asyncpg://postgres:your_password@localhost:5432/inklight
REDIS_URL=redis://localhost:6379/0
SECRET_KEY=your-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7
DEFAULT_AI_PROVIDER=openai
DEFAULT_AI_BASE_URL=https://api.openai.com/v1
DEFAULT_AI_KEY=sk-your-openai-api-key
DEFAULT_AI_MODEL=gpt-4o-mini
```

**4. 初始化数据库**：
```bash
python init_db.py
```

**5. 启动后端**：
```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

**6. 设置管理员**：
```sql
-- 连接到 PostgreSQL
psql -U postgres -d inklight
-- 将指定用户设为管理员
UPDATE users SET is_admin = true WHERE email = 'your@email.com';
```

### 8.3 前端配置

**1. 进入前端目录**：
```bash
cd inklight-frontend
```

**2. 安装依赖**：
```bash
npm install
```

**3. 开发模式启动**（默认 http://localhost:5173）：
```bash
npm run dev
```

**4. 生产构建**：
```bash
npm run build
```

**5. Vite 代理配置**（`vite.config.ts`，确保 API 请求转发到后端）：
```typescript
export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      }
    }
  }
})
```

### 8.4 生产环境部署建议

- 前端静态资源由 **Nginx** 提供，API 代理到后端
- 后端使用 **Gunicorn + Uvicorn Workers** 或 **Docker Compose** 部署
- PostgreSQL 启用 **pgvector** 扩展用于语义搜索
- 使用环境变量管理所有密钥，**不得**将 `.env` 文件提交到版本控制
- 生产环境务必修改默认 `SECRET_KEY`

---

## 9. 开发规范

### 9.1 命名约定

| 层级 | 约定 | 示例 |
|------|------|------|
| 后端文件 | snake_case | `literature_service.py` |
| 后端类名 | PascalCase | `LiteratureService` |
| 后端函数 | snake_case | `get_literature_by_id()` |
| 数据库模型 | 单数名词 | `Literature`, `User` |
| 数据库表名 | 小写复数 | `literatures`, `users` |
| 前端文件 | kebab-case | `literature-list.vue` |
| 前端组件 | PascalCase | `<LiteratureCard />` |
| 组合式函数 | use 前缀 | `useAuth()` |
| Pinia Store | use + 名词 + Store | `useAuthStore` |
| TypeScript 接口 | PascalCase | `ILiterature`, `NoteType` |

### 9.2 代码组织原则

- **后端**：路由层仅做参数校验和调用服务 → 服务层封装所有业务逻辑 → 模型层定义数据结构
- **前端**：组件单一职责 → 复杂逻辑抽离到 composables → API 封装在 `api/` 目录中
- **AI 调用**：所有 AI 调用通过 `core/ai_providers/` 统一抽象，不在业务代码中直接写死供应商
- **异步耗时任务**：通过 Celery/BackgroundTasks 异步执行，API 立即返回任务 ID 供前端轮询

### 9.3 安全要求

- 所有 API 输入使用 Pydantic 严格验证
- 密码使用 bcrypt 哈希存储
- JWT Secret Key 通过环境变量注入
- AI API Key 使用 AES 加密存储于数据库
- 前端不暴露任何 API 密钥，全部由后端代理调用
- 文件上传限制：最大 50MB，仅允许 PDF 格式

---

## 10. 许可证与贡献

本项目为内部开发用途。外部贡献请联系项目维护者。

---

*最后更新：2026-05-13*
