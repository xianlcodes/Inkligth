# ============================================================
# InkLight 基础依赖 —— 体积大、极少变动
# 单独分层安装，避免频繁重建时重复下载 PyTorch（~200MB）
# ============================================================

# 向量嵌入（自动拉取 PyTorch CPU 版）
sentence-transformers>=3.0.0
